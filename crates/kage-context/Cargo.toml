[package]
name = "kage-context"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Context Engine: DOM/network/log relevance scoring, secret scrubbing, and token budgeting"

[dependencies]
kage-core = { path = "../kage-core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
tracing = "0.1"
